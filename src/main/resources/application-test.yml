spring:
  application:
    name: sicc-api

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: false

# JWT Configuration for Testing
security:
  jwt:
    # Base64 encoded secret (min 256 bits)
    # This is: "test-secret-key-do-not-use-in-production-1234567890"
    secret-key: dGVzdC1zZWNyZXQta2V5LWRvLW5vdC11c2UtaW4tcHJvZHVjdGlvbi0xMjM0NTY3ODkwLWV4dHJhLWNoYXJz
    expiration-access: 900000        # 15 minutos
    expiration-refresh: 2592000000   # 30 días

logging:
  level:
    root: WARN
    cl.sicc.siccapi: INFO

