spring:
  datasource:
    url: jdbc:postgresql://db:5432/siccdb   # host = servicio "db" del compose
    username: sicc
    password: sicc
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: false
